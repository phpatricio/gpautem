<section class="content invoice">
  <div class="row">
    <div class="col-md-10">
      <%= form_for(@codigo, :html => { :role => "form" }) do |f| %>
        <% if @codigo.errors.any? %>
          <div class="alert alert-danger alert-dismissable" role="alert">
            <button type="button" class="close" data-dismiss="alert" aria-hidden="true">x</button>
            <h4><%= pluralize(@codigo.errors.count, "error") %> prohibited this codigo from being saved:</h4>

            <ul>
            <% @codigo.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
            </ul>
          </div>
        <% end %>

    
  <div class="form-group">
        <%= f.label :codigo_carrera %>
        <%= f.number_field :codigo_carrera, :class => "form-control" %>
      </div>
    
  <div class="form-group">
        <%= f.submit :class => "btn btn-primary" %>
      </div>
    <% end %>
    </div>
  </div>
</section> 