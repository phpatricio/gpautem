<h1>AREAS DE PRACTICA</h1>
<table border="1">
<tr>
	<th>NOMBRE</th>
</tr>
<% @areas.each do |f| %>
<tr>
	<td><%= f.nombre %></td>
	<td><%= link_to 'MOSTRAR', area_url(f) %></td>
	<td><%= link_to 'EDITAR', editar_area_url(f) %></td>
	<td><%= link_to 'ELIMINAR', eliminar_area_path(f),
                method: :delete,
                data: {confirm: '¿Estas seguro?'} %></td>
</tr>
<% end %>
